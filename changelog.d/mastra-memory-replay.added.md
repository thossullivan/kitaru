- Add opt-in Mastra streamed replay with isolated native thread memory. `createMemoryReplayAgent()` from `@zenml-io/kitaru-mastra/memory` records starting working and observational memory, ordered observation and reflection outputs, effective actor requests, controlled file bytes, and memory changes. Replay reuses those recorded outputs without reading or writing production memory. Requires Mastra core 1.67.0 and memory 1.30.0; existing history-only recordings must be recorded again to use this path.
