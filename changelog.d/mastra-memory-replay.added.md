- Add opt-in Mastra streamed replay with isolated native thread memory. `createMemoryReplayAgent()` records starting working and observational memory, effective actor requests, controlled file bytes, and ordered memory changes, then replays from that state without reading or writing production memory. Requires Mastra core 1.67.0 and memory 1.30.0; existing history-only recordings must be recorded again to use this path.
